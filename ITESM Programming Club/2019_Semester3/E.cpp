#include <chrono>
#include<iostream> 
#include <stdio.h>
using namespace std; 

int main() {
    int queries;
    scanf("%i", &queries);

    int males[queries], females[queries];
    for (int i=0; i<queries; i++) scanf("%i %i", &males[i], &females[i]);

    while (queries != 0) {
        if () {

        }

        for (int i=0; i<0; i++) {

        }
    }
    return 0;
}